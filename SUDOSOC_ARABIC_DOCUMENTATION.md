# التوثيق الشامل لمشروع SudoSoc (نظام كشف ومنع التطفل المدعوم بالذكاء الاصطناعي)

**اسم المشروع:** SudoSoc Security Platform
**الإصدار:** 2.0 (نسخة الذكاء الاصطناعي الهجين - Stacking Ensemble & LLM)
**الحالة:** جاهز للإنتاج والتطبيق العملي

---

## 1. الهدف من المشروع (Project Goal)
يهدف مشروع **SudoSoc** إلى توفير نظام متقدم لاكتشاف ومنع الاختراقات الشبكية (IDS/IPS) باستخدام تقنيات هجينة تجمع بين القواعد الأمنية التقليدية (Heuristics)، ونماذج تعلم الآلة المتقدمة (Machine Learning)، والذكاء الاصطناعي التوليدي (LLMs).
الهدف الرئيسي هو حماية الشبكات والأنظمة في الوقت الفعلي من الهجمات المعقدة (مثل هجمات الحرمان من الخدمة DoS، الفحص الاستطلاعي Probes، والهجمات الموجهة) مع القدرة على تقليل الإنذارات الكاذبة (False Positives)، وتحليل البيانات المشفرة دون فك تشفيرها، واتخاذ إجراءات تلقائية لحظر المهاجمين.

---

## 2. المميزات الرئيسية (Core Features)
1. **محرك الكشف الهجين (Hybrid Detection Engine):**
   - **الطبقة الأولى (القواعد):** فحص سريع للبيانات ومطابقتها مع أنماط التهديدات المعروفة.
   - **الطبقة الثانية (تعلم الآلة):** استخدام نماذج مجمعة (Stacking Ensemble) تجمع بين خوارزميات (Random Forest و XGBoost) لاكتشاف الهجمات المعقدة والجديدة بدقة عالية.
   - **الطبقة الثالثة (الذكاء التوليدي LLM):** استخدام نماذج لغوية ضخمة (عبر HuggingFace أو Groq) كـ "محلل أمني" للتحقق من التهديدات المكتشفة وتقليل الإنذارات الكاذبة، وشرح سبب اعتبار النشاط تهديداً.
2. **الكشف عن الشذوذ في الترافيك المشفر:** يعتمد على تحليل عشوائية البيانات (Entropy) وتوقيت الحزم لاكتشاف الهجمات داخل الاتصالات المشفرة (TLS/SSL) دون الحاجة لكسر التشفير وخرق الخصوصية.
3. **الاستجابة التلقائية الفورية (Automated Response):** تكامل مع جدار الحماية (Firewall) الخاص بنظام التشغيل لحظر عناوين IP الخبيثة (Active Blocking) بشكل تلقائي عند التأكد من وجود هجوم.
4. **حلقة الملاحظات والتعلم المستمر (Feedback Loop & Online Learning):** السماح للمحلل الأمني بتصحيح قرارات النظام، مما يسمح للنموذج بإعادة التدريب والتكيف مع التهديدات في بيئة الشبكة الحالية (مواجهة Concept Drift).
5. **الشفافية والتفسير (Explainable AI):** توضيح دقيق للأسباب التي دفعت الذكاء الاصطناعي لحظر اتصال معين، مما يسهل عمل فرق الاستجابة للحوادث (SOC).
6. **لوحة تحكم تفاعلية (Real-time Dashboard):** واجهة مستخدم رسومية تعرض التنبيهات لحظة بلحظة مع إحصائيات دقيقة ورسوم بيانية باستخدام Streamlit.

---

## 3. شرح ملفات المشروع (Project Files Breakdown)
يحتوي المشروع على عدة ملفات برمجية، كل ملف يؤدي دوراً محدداً في النظامية المعمارية للمشروع:

| اسم الملف | الوظيفة الأساسية | التفاصيل |
| :--- | :--- | :--- |
| **`realtime_ids.py`** | **المحرك الرئيسي (Main Engine)** | الملف الأساسي المسؤول عن التقاط البيانات من الشبكة في الوقت الفعلي (باستخدام Scapy)، استخراج الخصائص، إرسالها لنماذج الذكاء الاصطناعي للتنبؤ، وتنفيذ أوامر الحظر الآلي في جدار الحماية. |
| **`ids_ips_trainer.py`** | **تدريب النماذج (Model Training)** | يحتوي على مسار تدريب نماذج الذكاء الاصطناعي (Pipeline). يقوم بقراءة البيانات المجهزة، وتدريب مجموعة النماذج (RF و XGBoost وغيرها) وحفظها للاستخدام الفعلي. |
| **`advanced_trainer.py`** | **التدريب المتقدم** | نسخة متقدمة من ملف التدريب قد تحتوي على خوارزميات إضافية لتحسين النماذج والتعامل مع البيانات المعقدة. |
| **`dataset_manager.py`** | **إدارة البيانات (Dataset Manager)** | مسؤول عن التعامل مع مجموعات البيانات الكبيرة (Datasets مثل CIC-IDS)، تنزيلها، معالجتها، توحيد التسميات (Label Mapping)، وتجهيزها لتكون صالحة لعملية التدريب. |
| **`dashboard.py`** | **لوحة التحكم (Monitoring UI)** | واجهة المستخدم الرسومية باستخدام Streamlit. توفر مراقبة حية لعملية كشف التسلل، وتعرض رسوم بيانية لإحصاءات الترافيك، والإنذارات الحديثة. |
| **`llm_analyzer.py`** | **محلل الذكاء التوليدي (AI Analyzer)** | يتصل بواجهات برمجة التطبيقات (APIs) للنماذج اللغوية الضخمة (مثل HuggingFace أو Groq) لفحص بيانات الاتصال المشبوه وإعطاء تحليل بشري مقروء حول طبيعة الهجوم للتأكد من عدم وجود إنذار كاذب. |
| **`feedback_loop.py`** | **نظام التصحيح (Feedback Loop)** | ملف مهم جداً لإدارة التقييمات البشرية (Human-in-the-loop)، يقوم بأخذ التصحيحات من محلل النظام وتحديث النموذج (Online Learning) لجعله أكثر دقة مع مرور الوقت. |
| **`explainability.py`** | **تفسير القرارات (Explainable AI)** | يوفر آليات (مثل SHAP) لشرح سبب اتخاذ النموذج لقرار معين (ما هي الخصائص التي أثرت في القرار بشكل أكبر). |
| **`drift_monitor.py`** | **مراقب الانحراف (Concept Drift)** | يراقب أداء النظام بمرور الوقت لاكتشاف متى تتغير أنماط الهجمات (Drift)، ويُطلق تنبيهات لضرورة إعادة تدريب النموذج ببيانات أحدث. |
| **`secure_sniffer.py`** | **اللاقط الآمن** | وحدة مصممة لالتقاط الاتصالات وتجهيزها، وربما تكون مسؤولة عن التكامل مع بروكسي لفك تشفير البيانات إذا تطلب الأمر (MITM). |
| **`report_generator.py`** | **مولّد التقارير** | توليد تقارير شاملة (بصيغ مثل Excel أو PDF) توثق الهجمات التي تم صدها وإحصائيات النظام لتقديمها للمدراء. |
| **`ids_alerts.jsonl`** | **سجل الإنذارات** | ملف تخزين (قاعدة بيانات نصية) يُسجل فيه النظام كل تنبيه وكل هجوم تم رصده وتفاصيله الدقيقة. |

---

## 4. طريقة التشغيل (How to Use)

لتشغيل المشروع بشكل صحيح، اتبع الخطوات التالية بالترتيب:

### الخطوة الأولى: تهيئة البيئة وتثبيت المتطلبات
تأكد من توافر بيئة بايثون، ثم افتح موجّه الأوامر (Terminal) في مجلد المشروع وقم بتثبيت المكتبات اللازمة:
```bash
pip install -r requirements.txt
```
*(أو تثبيت المكتبات يدوياً مثل: scapy, pandas, scikit-learn, xgboost, streamlit, requests)*

### الخطوة الثانية: تدريب نموذج الذكاء الاصطناعي (Training)
قبل تشغيل النظام الرقابي، يجب تدريب أو بناء النموذج باستخدام البيانات المتوفرة:
```bash
python ids_ips_trainer.py
```
*(هذا الأمر سيقوم بمعالجة البيانات وتوليد ملفات النموذج المحفوظة في المجلدات المخصصة)*

### الخطوة الثالثة: تشغيل محرك الحماية الفوري (Real-Time IDS/IPS)
**ملاحظة هامة:** يجب تشغيل هذا الملف بصلاحيات المسؤول (Run as Administrator في ويندوز أو sudo في لينكس) لكي يتمكن من التعديل على جدار الحماية وقراءة بطاقات الشبكة:
```bash
python realtime_ids.py
```
سيبدأ المحرك بالتقاط وتحليل حركة مرور الشبكة بشكل فوري.

### الخطوة الرابعة: تشغيل لوحة التحكم (Dashboard)
افتح نافذة أوامر (Terminal) أخرى في نفس المجلد، وقم بتشغيل واجهة المراقبة:
```bash
streamlit run dashboard.py
```
سيفتح متصفح الويب تلقائياً وتظهر أمامك لوحة التحكم لمراقبة الشبكة.

---

## 5. النتائج المتوقعة (Results & Benefits)
- **دقة عالية وأمان فوري:** حماية استباقية للشبكات المضيفة قادرة على التفرقة بين الزيارات الطبيعية ومحاولات الاختراق المعقدة بدقة تتجاوز 95%.
- **استجابة دون تدخل بشري:** إيقاف التهديد في اللحظة التي يُكتشف فيها بواسطة الـ IPS (إضافة قواعد حجب فورية على مستوى نظام التشغيل).
- **الحد من الإنذارات المزعجة (Low False Positives):** بفضل تكامل نماذج الـ LLM مع النظام، يتم التحقق مرتين من الاتصالات المشبوهة، مما يمنع حظر الاتصالات الآمنة بالخطأ.
- **نظام ذكي متطور:** عبر وحدات الـ Drift Monitor و Feedback Loop، النظام لا يتقادم بل يزداد ذكاءً وقدرة على فهم طبيعة شبكتك المحددة مع مرور الوقت بفضل التغذية الراجعة.
- **رؤية شاملة للقيادة:** لوحة التحكم الدقيقة والتقارير الدورية تجعل الإدارة الأمنية أسهل للمختصين وصناع القرار.
